---
id: KL-001
name: Kyverno Unsafe Pod Creation Log
description: Kubernetes API를 통해 특권 Pod 또는 호스트 네임스페이스 접근이 가능한 위험 Pod 스펙이 생성된 행위를 설명하는 Kyverno 정책 위반 로그
techniques: T1611
---

# KL-001 Kyverno Unsafe Pod Creation Log

| Field | Value |
| --- | --- |
| Log ID | KL-001 |
| Source | SB-04 K8s Node |
| Representative Path | `/var/log/nodefall/nodefall-events.log` |
| Collection | Wazuh agent file input (`/var/log/nodefall/nodefall-events.log`) |
| Primary Use | 특권 Pod 생성 및 호스트 네임스페이스 접근이 가능한 위험 Pod 스펙 생성 행위 확인 |

## 공격 행위 요약

KL-001은 NodeFall 캠페인에서 공격자가 Kubernetes API 권한을 악용해 컨테이너 탈출에 사용할 수 있는 위험한 Pod를 생성한 행위를 설명하는 Kyverno 기반 로그이다.

공격자는 확보한 Kubernetes 자격 증명과 권한을 이용해 `privileged`, `hostPID`, `hostNetwork`, `hostIPC`, `hostPath`와 같은 위험한 Pod 스펙을 포함한 Pod 생성을 시도했다. 이러한 설정은 컨테이너가 호스트의 네임스페이스나 파일 시스템에 접근할 수 있게 만들어 컨테이너 격리 경계를 약화시킨다.

Kyverno는 Kubernetes API admission 단계에서 Pod 생성 또는 수정 요청을 검사하며, 위험한 스펙이 포함된 요청이 정책 조건과 매칭되면 PolicyViolation 이벤트를 생성한다. 이 로그는 실제 런타임 탈출 행위가 발생하기 전, 공격자가 호스트 접근을 가능하게 하는 실행 환경을 준비했다는 증거로 사용된다.

## 로그 발생 위치

| 구분 | 위치 | 설명 |
| --- | --- | --- |
| Kubernetes API | kube-apiserver AdmissionReview | 공격자의 Pod 생성/수정 요청이 admission 단계로 전달됨 |
| Kyverno | ClusterPolicy `nodefall-kl-001-suspicious-pod` | `validate.deny.conditions`가 Pod 스펙의 위험 설정을 검사 |
| Kyverno | `validate.message` | 조건 매칭 시 `[KL-001] Suspicious Pod Created` 메시지를 가진 PolicyViolation 이벤트 생성 |
| Kubernetes Event | `reason=PolicyViolation` | Kyverno 정책 위반 결과가 Kubernetes Event로 저장됨 |
| K8s Node | `/usr/local/bin/nodefall-kyverno-event-watch.sh` | watcher가 PolicyViolation 이벤트를 감시 |
| K8s Node | `/var/log/nodefall/nodefall-events.log` | watcher가 Kyverno 이벤트를 파일 로그로 저장 |

## 수집 방식

| 구분 | 방식 |
| --- | --- |
| Kyverno 탐지 기준 | ClusterPolicy의 `validate.deny.conditions`가 `privileged`, `hostPID`, `hostNetwork`, `hostIPC`, `hostPath` 설정을 검사 |
| Kyverno 탐지 메시지 | 조건 매칭 시 `validate.message` 값인 `[KL-001] Suspicious Pod Created`가 PolicyViolation 이벤트에 포함됨 |
| Kubernetes Event | Kyverno가 `reason=PolicyViolation` 이벤트를 생성 |
| nodefall watcher | `/usr/local/bin/nodefall-kyverno-event-watch.sh`가 PolicyViolation 이벤트를 감시 |
| 파일 저장 | watcher가 `/var/log/nodefall/nodefall-events.log`에 이벤트를 저장 |
| Wazuh agent | K8s node의 `/var/ossec/etc/ossec.conf`에서 `/var/log/nodefall/nodefall-events.log`를 `syslog` 형식으로 수집 |
| Wazuh manager | Docker 컨테이너 `single-node_wazuh.manager_1` 내부 `/var/ossec/etc/rules/local_rules.xml`에서 KL-001 룰과 매칭 |
| Wazuh Dashboard | 매칭된 alert를 `[KL-001] Suspicious Pod Created` 이벤트로 표시 |
